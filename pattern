no_of_row = input('enter tne no. of rows: ')
no_of_row = int(no_of_row)
i = 0
n = 1
for _ in range(i,no_of_row):
  for _ in range(i,n):
    print("* ", end = '')
  print('')
  if n == no_of_row:
    break
  n = n + 1 #increment of flag by 1 till n = no of rows
